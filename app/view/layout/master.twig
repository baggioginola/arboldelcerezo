<!DOCTYPE html>
<html>
<head>
    {% block head %}
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <title>{% block title %}{% endblock %} - Árbol del Cerezo</title>
        <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link rel="stylesheet" href="{{ settings.CSS }}styles.css">

        <!-- jQuery library -->
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>

        <!-- Latest compiled JavaScript -->
        <script src="{{ settings.JS }}custom/config.js" type="text/javascript"></script>

        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
        <script src="//code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
    {% endblock %}
</head>

<body id="{% block body_id %}{% endblock %}">
<nav id="header" class="navbar navbar-default navbar-fixed-top new-header-mobile" role="navigation">

    <div class="container logoMenuBlock" itemscope itemtype="https://schema.org/Organization">
        <meta itemprop="name" content="Árbol de Cerezo"/>
        <div class="navbar-header">

            <a class="navbar-brand" href="{{ settings.DOMAIN }}" itemprop="url">
                <img itemprop="logo" alt="Árbol de Cerezo" title="Ärbol de Cerezo"
                     src="{{ settings.IMG }}logo.png" width=""/>
                <span>Árbol de Cerezo</span>
            </a>


            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>

        </div>

        <div id="navbar" class="navbar-collapse collapse" aria-expanded="true">
            <ul class="nav navbar-nav">
                <li>
                    <form id="searchbox" action="{{ settings.DOMAIN }}buscar">
                        <div class="form-group">
                            <input type="text" class="form-control" placeholder="Buscar" autocomplete="off"
                                   required="required"
                                   type="text" {% if q is defined %} value="{{ q }}" {% endif %} name="q">
                        </div>
                        <button type="submit" class="btn btn-default">Aceptar</button>
                    </form>
                </li>
                <li class="active"><a href="{{ settings.DOMAIN }}">Inicio</a></li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                       aria-expanded="false">Categorías<span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        {% for category  in categories %}
                            <li><a href="{{ settings.DOMAIN }}categoria/{{ category.id }}">{{ category.nombre }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                </li>
                <li><a href="{{ settings.DOMAIN }}quienes-somos">¿Quiénes somos?</a></li>
                <li><a href="{{ settings.DOMAIN }}contacto">Contacto</a></li>
                <li><a href="{{ settings.DOMAIN }}carrito-compra">Carrito de compras</a></li>
            </ul>
        </div>

        <div id="menuOptions" class="navbar-collapse collapse" style="float: right;">
            <div style="float: right;">
                <div class="searchButton navbar-form">
                    <form id="searchbox" action="{{ settings.DOMAIN }}buscar">
                        <div class="form-group">
                            <input type="text" class="form-control" placeholder="Buscar" autocomplete="off"
                                   required="required"
                                   type="text" {% if q is defined %} value="{{ q }}" {% endif %} name="q">
                        </div>
                        <button type="submit" class="btn btn-default">Aceptar</button>
                    </form>
                </div>
                <div class="social">
                    <a id="face" target="_blank" href=" https://www.facebook.com/arboldeceresso/"></a>
                </div>
                <div class="cart">
                    <div class="cart_img"><img src="{{ settings.IMG }}cart.png" width="28px"/></div>
                    <div class="cart_link">
                        <a href="{{ settings.DOMAIN }}carrito-compra" id="cart_productos">
                            {% if cart_products == 0 %}VACÍO {% else %}{{ cart_products }} PRODUCTO(S){% endif %}
                        </a>
                    </div>
                </div>
            </div>
            <div style="clear:both;"></div>
            <div class="nav-area">
                <div class="nav-bottom">
                    <ul>
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                               aria-expanded="false">Categorías<span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                {% for category  in categories %}
                                    <li><a href="{{ settings.DOMAIN }}categoria/{{ category.id }}">{{ category.nombre }}</a>
                                    </li>
                                {% endfor %}
                            </ul>
                        </li>
                        <li style="padding-left: 20px;">
                            <a href="{{ settings.DOMAIN }}quienes-somos">¿Quiénes Somos?</a>
                        </li>
                        <li id="limenu_4" style="padding-left: 20px;">
                            <a href="{{ settings.DOMAIN }}contacto">CONTACTO</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>
{% block content %}{% endblock %}

<div class="footer">
    <div class="container-fluid contentFooter">
        <div class="container">
            <div class="row">
                <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12" style="padding-top: 10px;">
                    <legend style="color: #FFFFFF;"><span class="glyphicon glyphicon-globe"
                                                          style="font-size:65%; padding-right:10px;"></span>Contacto
                    </legend>
                    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
                        <form>
                            <address>
                                <strong style="color: #fff">E-mail</strong><br>
                                <a href="mailto:#">informes@arboldecerezo.com.mx</a>
                            </address>
                        </form>
                    </div>
                    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
                        <form>
                            <address style="color: #FFF;">
                                <abbr title="Phone">
                                    Teléfonos:</abbr><br>
                                01 (222) 416 0811<br>
                            </address>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>